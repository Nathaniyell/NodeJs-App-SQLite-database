{
  "name": "first-backend-project",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env -- ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A dockerized fullstack application that uses a NodeJs backend, a PostgreSQL database, a Prisma ORM, and JWT Authentication",
  "dependencies": {
    "bcryptjs": "^3.0.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
